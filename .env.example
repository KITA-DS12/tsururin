# コピーして .env を作成し、自分の値を入れる
# .env は .gitignore で除外済み

# S3 バケット名（ドメインと同名を推奨）
S3_BUCKET=tsururin.mu-k.net

# CloudFront ディストリビューション ID（例: E1A2B3C4D5E6F7）
CF_DISTRIBUTION_ID=

# AWS プロファイル（複数プロファイルを使い分けている場合のみ）
AWS_PROFILE=default
